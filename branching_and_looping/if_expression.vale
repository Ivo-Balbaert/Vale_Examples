fn main() export {
  n = 1;
  m = 2;
  width =
    if (n < m) {
      = 42;
    } else {
      = 150;
    };
    println(width); // => 42

  something =
    if (4 < 7) {
      println("hi!");  // => hi!
      = 7;    // = ;  needed when multiple statements in block
    } else {
      10      // works because its the only statement in the block
    };
  println(something); // => 7
} 


