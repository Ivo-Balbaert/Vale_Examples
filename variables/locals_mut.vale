import stdlib.*;

exported func main() {
  n int = 73;
  // mutate variable:
  set n = 42;
  println(n); // => 42
  
  // variable with type inference:
  x = "Europe!";
  set x = "Antarctica!";
  println(x); // => Antarctica!
}