// 6 - Iterate over list values
// Do something with each item x of an array-like collection items, regardless indexes.

import stdlib.*;

exported func main() {
  lst = [#]["Storm", "Earth", "Fire"]; // this is a static-sized array in Vale
  foreach x in lst {
    println(x);
  }
}

// =>
// Storm
// Earth
// Fire