// 5- Create a 2D Point data structure
// Declare a container type for two floating-point numbers x and y
// Feb 12 2021 - no print for float exist, --> int
struct Point {
  x int;
  y int;
}

fn main() export {
  p1 = Point(2, 3);
  println(p1.x); // => 2
  println(p1.y); // => 3
}