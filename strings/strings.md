**strings.vale**: declaring strings, length, concatenating strings, converting other types to string

**string_functions.vale**: common string functionality like <=>, slicing, find, startsWith, endsWith

split, join: see **lists**
