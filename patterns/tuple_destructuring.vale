import stdlib.*;

exported func main() {
  n = 5;
  [g, h] = double(5);
  println("The double of " + g + " is: " + h);
  // => The double of 5 is: 10
}

func double(x int) infer-ret {  
  ret (x, x * 2);
}

